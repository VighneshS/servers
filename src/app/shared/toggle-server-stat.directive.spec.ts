import { BlankCheckDirective } from './blank-check.directive';

describe('BlankCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new BlankCheckDirective();
    expect(directive).toBeTruthy();
  });
});
